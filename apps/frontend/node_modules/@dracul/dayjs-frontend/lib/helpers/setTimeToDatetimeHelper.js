"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _Dayjs = _interopRequireDefault(require("../utils/Dayjs"));

var _dayjs = require("dayjs");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 *
 * @param {Dayjs} sourceDate
 * @param {string} newTime
 * @return {Dayjs}
 */
const setTimeToDatetimeHelper = (sourceDate, newTime) => {
  if (sourceDate === null || sourceDate === '') {
    sourceDate = (0, _Dayjs.default)();
  } else if (!(0, _dayjs.isDayjs)(sourceDate)) {
    throw new Error("Date is not a Dayjs instance");
  }

  let timeSplit = newTime.split(":");
  let hour = parseInt(timeSplit[0]);
  let minute = parseInt(timeSplit[1]);
  return (0, _Dayjs.default)(sourceDate).hour(hour).minute(minute);
};

var _default = setTimeToDatetimeHelper;
exports.default = _default;